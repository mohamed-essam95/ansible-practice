events {
    worker_connections 1024;
}
http {
    server {
        listen {{ nginx_port }};
        server_name localhost;

        location / {
            root {{ nginx_root }};
            index index.html;
        }
    }
}